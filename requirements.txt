flask>=3.1
python-dotenv>=1.0
pymysql>=1.1
DBUtils>=3.1.2
authlib==1.7.2
requests>=2.34
# ⚠️ 상한 없는 하한(>=0.40)으로 두면 재빌드 시점의 최신이 설치된다.
# 2026-08-23 홈서버 재빌드에서 실제로 0.125.0 -> 1.0.0(메이저)으로 올라가
# Messages.create() 의 temperature 인자가 사라져 영수증 OCR 이 죽었다(#225).
# 실제로 돌던 버전으로 고정한다. 1.0 마이그레이션(temperature -> output_config)은
# 호출부 4곳과 테스트를 함께 검증해야 하므로 분리한다(#221).
anthropic==0.125.0
gunicorn>=23.0
APScheduler>=3.10,<4.0
pytest>=8.0
Pillow>=10.0,<12.0
